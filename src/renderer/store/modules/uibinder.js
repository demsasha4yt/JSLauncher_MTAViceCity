var child = require('child_process').execFile;

const state = {
}

const mutations = {

}

const actions = {
    LaunchButtonHandle(state, nickname)
    {
        // Handling in main process by Subscribe to Action
    },
    SettingsButtonHandle(state)
    {
        // Handling in main process by Subscribe to Action
    },
    CloseBtnHandle(state) {
        // Handling in main process by Subscribe to Action
    },
    MinimizeBtnHandle(state) {
        // Handling in main process by Subscribe to Action
    }
}

const getters = {

}

export default {
    state,
    mutations,
    actions,
    getters
}
